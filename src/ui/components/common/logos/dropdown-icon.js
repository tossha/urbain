import React from "react";
import { ReactComponent as ChevronDown } from "../images/chevron-down.svg";

const DropdownIcon = ({ className }) => <ChevronDown className={`dropdown-icon ${className}`} />;

export default DropdownIcon;
